// InfotainmentScreen.qml
// Compatibility entry point kept for older imports. The active application
// frame lives in AppShell so page routing stays in one place.

import QtQuick
import App.Screens

AppShell {
}
